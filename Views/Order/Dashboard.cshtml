@* @{
    ViewData["Title"] = "Dashboard";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - PharmacyApp</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            background: #f9fafc;
            color: #1e293b;
        }
        .navbar {
            background: #667eea;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            margin-left: 1rem;
            font-weight: 600;
        }
        .hero {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            text-align: center;
            padding: 4rem 2rem;
        }
        section {
            padding: 3rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        section h2 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 2rem;
            color: #111827;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        .card {
            background: white;
            border-radius: 16px;
            padding: 1.5rem;
            box-shadow: 0 8px 20px rgba(0,0,0,0.05);
            transition: transform 0.3s ease;
            text-align: center;
        }
        .card:hover { transform: translateY(-8px); }
        .card .icon {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 1rem;
        }
        .card h5 { margin-bottom: .5rem; }
        .card p { color: #64748b; margin-bottom: 1rem; }
        .card a {
            display: inline-block;
            padding: 10px 18px;
            border-radius: 8px;
            text-decoration: none;
            color: white;
            background: #667eea;
            font-weight: 600;
        }
        footer {
            background: #111827;
            color: #d1d5db;
            text-align: center;
            padding: 1.5rem;
            margin-top: 3rem;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar">
        <div class="logo"><i class="fas fa-capsules"></i> PharmacyApp</div>
        <div>
            <a asp-controller="Account" asp-action="Profile"><i class="fas fa-user"></i> Profile</a>
            <a asp-controller="Account" asp-action="Logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </div>

    <!-- Hero -->
    <section class="hero">
        <h1>Welcome, @User.Identity.Name 👋</h1>
        <p>Your one-stop pharmacy dashboard — order meds, track prescriptions, and connect with pharmacists near you.</p>
    </section>

    <!-- Quick Actions -->
    <section>
        <h2>Quick Actions</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-pills"></i></div>
                <h5>Browse Drugs</h5>
                <p>Explore our medicine catalog.</p>
                <a asp-controller="Drugs" asp-action="Index">Shop Now</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-file-prescription"></i></div>
                <h5>Upload Prescription</h5>
                <p>Upload your doctor’s prescription.</p>
                <a asp-controller="Prescription" asp-action="Upload">Upload</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-shopping-bag"></i></div>
                <h5>My Orders</h5>
                <p>Track and manage your orders.</p>
                <a asp-controller="Order" asp-action="MyOrders">View Orders</a>
            </div>
        </div>
    </section>

    <!-- Search Pharmacies -->
    <section style="background:#f3f4f6;">
        <h2>Find Pharmacies</h2>
        <p style="text-align:center;max-width:600px;margin:0 auto 2rem;">
            Search by pharmacy name, local government area, or street to locate trusted pharmacists near you.
        </p>
        <div style="text-align:center;">
            <a asp-controller="Pharmacy" asp-action="Search" class="btn btn-primary">Search Pharmacies</a>
        </div>
    </section>

    <!-- My Prescriptions -->
    <section>
        <h2>My Prescriptions</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-notes-medical"></i></div>
                <h5>View Prescriptions</h5>
                <p>See drugs prescribed by pharmacists after complaints.</p>
                <a asp-controller="Prescription" asp-action="MyPrescriptions">Check Now</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-exclamation-circle"></i></div>
                <h5>Lodge Complaint</h5>
                <p>Report symptoms or request pharmacist review.</p>
                <a asp-controller="Complaint" asp-action="Create">Lodge Complaint</a>
            </div>
        </div>
    </section>

    <!-- Support -->
    <section style="background:#f3f4f6;">
        <h2>Need Help?</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-user-cog"></i></div>
                <h5>Account Settings</h5>
                <p>Manage your profile and preferences.</p>
                <a asp-controller="Account" asp-action="Profile">Manage</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-comments"></i></div>
                <h5>Live Chat</h5>
                <p>Chat with a certified pharmacist.</p>
                <a asp-controller="Support" asp-action="Chat">Start Chat</a>
            </div>
        </div>
    </section>

    <footer>
        <p>© @DateTime.Now.Year PharmacyApp. All Rights Reserved.</p>
    </footer>
</body>
</html>  *@


@{
    ViewData["Title"] = "Dashboard";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - PharmacyApp</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            background: #f9fafc;
            color: #1e293b;
        }
        .navbar {
            background: #667eea;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            margin-left: 1rem;
            font-weight: 600;
        }
        .hero {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            text-align: center;
            padding: 4rem 2rem;
        }
        section {
            padding: 3rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        section h2 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 2rem;
            color: #111827;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        .card {
            background: white;
            border-radius: 16px;
            padding: 1.5rem;
            box-shadow: 0 8px 20px rgba(0,0,0,0.05);
            transition: transform 0.3s ease;
            text-align: center;
        }
        .card:hover { transform: translateY(-8px); }
        .card .icon {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 1rem;
        }
        .card h5 { margin-bottom: .5rem; }
        .card p { color: #64748b; margin-bottom: 1rem; }
        .card a {
            display: inline-block;
            padding: 10px 18px;
            border-radius: 8px;
            text-decoration: none;
            color: white;
            background: #667eea;
            font-weight: 600;
            margin: 3px;
        }
        footer {
            background: #111827;
            color: #d1d5db;
            text-align: center;
            padding: 1.5rem;
            margin-top: 3rem;
        }
        .btn-primary {
            padding: 10px 18px;
            border-radius: 8px;
            text-decoration: none;
            color: white;
            background: #667eea;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar">
        <div class="logo"><i class="fas fa-capsules"></i> PharmacyApp</div>
        <div>
            <a asp-controller="Account" asp-action="Profile"><i class="fas fa-user"></i> Profile</a>
            <a asp-controller="Account" asp-action="Logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </div>

    <!-- Hero -->
    <section class="hero">
        <h1>Welcome, @User.Identity.Name 👋</h1>
        <p>Your one-stop pharmacy dashboard — order meds, track prescriptions, and connect with pharmacists near you.</p>
    </section>

    <!-- Quick Actions -->
    <section>
        <h2>Quick Actions</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-pills"></i></div>
                <h5>Browse Drugs</h5>
                <p>Explore our medicine catalog.</p>
                <a asp-controller="Drugs" asp-action="Index">Shop Now</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-file-prescription"></i></div>
                <h5>Upload Prescription</h5>
                <p>Upload your doctor’s prescription.</p>
                <a asp-controller="Prescription" asp-action="Upload">Upload</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-shopping-bag"></i></div>
                <h5>My Orders</h5>
                <p>Track and manage your orders.</p>
                <a asp-controller="Order" asp-action="MyOrders">View Orders</a>
            </div>
        </div>
    </section>

    <!-- Find Nearest Pharmacies -->
    <section style="background:#f3f4f6;">
        <h2>Nearest Pharmacies</h2>
        <p style="text-align:center;max-width:600px;margin:0 auto 1rem;">
            Based on your location, here are the closest pharmacies near you.
        </p>
        <div style="text-align:center;">
            <button onclick="findNearestPharmacies()" class="btn-primary">Detect My Location</button>
        </div>
        <div class="grid" id="nearestPharmacies" style="margin-top:2rem;">
            <!-- Pharmacies will load here -->
        </div>
    </section>

    <!-- My Prescriptions -->
    <section>
        <h2>My Prescriptions</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-notes-medical"></i></div>
                <h5>View Prescriptions</h5>
                <p>See drugs prescribed by pharmacists after complaints.</p>
                <a asp-controller="Prescription" asp-action="MyPrescriptions">Check Now</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-exclamation-circle"></i></div>
                <h5>Lodge Complaint</h5>
                <p>Report symptoms or request pharmacist review.</p>
                <a asp-controller="Complaint" asp-action="Create">Lodge Complaint</a>
            </div>
        </div>
    </section>

    <!-- Support -->
    <section style="background:#f3f4f6;">
        <h2>Need Help?</h2>
        <div class="grid">
            <div class="card">
                <div class="icon"><i class="fas fa-user-cog"></i></div>
                <h5>Account Settings</h5>
                <p>Manage your profile and preferences.</p>
                <a asp-controller="Account" asp-action="Profile">Manage</a>
            </div>
            <div class="card">
                <div class="icon"><i class="fas fa-comments"></i></div>
                <h5>Live Chat</h5>
                <p>Chat with a certified pharmacist.</p>
                <a asp-controller="Support" asp-action="Chat">Start Chat</a>
            </div>
        </div>
    </section>

    <footer>
        <p>© @DateTime.Now.Year PharmacyApp. All Rights Reserved.</p>
    </footer>

    <!-- JS for Nearest Pharmacies -->
    <script>
        function findNearestPharmacies() {
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(showPosition, showError);
            } else {
                alert("Geolocation is not supported by this browser.");
            }
        }

        function showPosition(position) {
            const lat = position.coords.latitude;
            const lon = position.coords.longitude;

            fetch(`/Pharmacy/GetNearest?lat=${lat}&lon=${lon}`)
                .then(res => res.json())
                .then(data => {
                    let container = document.getElementById("nearestPharmacies");
                    container.innerHTML = "";

                    if (data.length === 0) {
                        container.innerHTML = "<p>No pharmacies found nearby.</p>";
                    } else {
                        data.forEach(pharmacy => {
                            container.innerHTML += `
                              <div class="card">
                                <div class="icon"><i class="fas fa-clinic-medical"></i></div>
                                <h5>${pharmacy.name}</h5>
                                <p>${pharmacy.address}</p>
                                <p><b>${pharmacy.distance.toFixed(1)} km away</b></p>
                                <a href="/Order/Create?pharmacyId=${pharmacy.id}">Order Drugs</a>
                                <a href="/Complaint/Create?pharmacyId=${pharmacy.id}" style="background:#ef4444">Lodge Complaint</a>
                              </div>
                            `;
                        });
                    }
                });
        }

        function showError(error) {
            alert("Unable to get location. Please enable location services.");
        }
    </script>
</body>
</html>
